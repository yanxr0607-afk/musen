# 依赖
node_modules/

# 本地环境变量（含密钥，切勿提交）
.env

# 运行时数据：部署到云平台后会重新种子化；
# 含 admin 密码、用户/订单/支付配置，切勿提交；
# 如需长期保存，请在平台上挂载「持久化卷」并设 DATA_DIR 指向该卷路径。
data/

# 本地一次性部署/隧道脚本（含敏感信息，切勿提交）
deploy-opc-oneline.txt
persist-tunnel-oneline.txt

# 本仓库的本地交付说明文档（非部署所需，保持仓库整洁）
overview-*.md
